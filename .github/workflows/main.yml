name: "bot-keep-alive"

on: {schedule: [{cron: "*/5 * * * *"}]}

jobs:
   { ping:
      { runs-on: "ubuntu-latest"
      , steps:
      [
         { name: "ping the repl.it server"
         , run: "curl https://UnBonkBot.coalpha.repl.co"
         }
      ]
      }
   }
